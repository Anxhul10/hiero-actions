name: Verify Commits

on:
  workflow_dispatch:
    inputs:
      pr_number:
        required: true
        type: string

      repo_owner:
        required: true
        type: string

      repo_name:
        required: true
        type: string

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run verification script
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ inputs.pr_number }}
          TARGET_OWNER: ${{ inputs.repo_owner }}
          TARGET_REPO: ${{ inputs.repo_name }}
        with:
          script: |
            const script = require('./.github/scripts/verify-commits.cjs');

            await script({
              github,
              context
            });